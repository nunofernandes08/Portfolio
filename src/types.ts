export interface ThemeInterface {
    themeLight: any,
    themeDark: any,
    themeLightRed: any,
}

export enum Themes {
    LIGHT = "light",
    DARK = "dark",
    LIGHTRED = "lightRed"
}

export enum Paths {
    SPOTIFY = "/spotify"
}